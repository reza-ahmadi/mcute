// #include <iostream>
// #include <stdio.h>

// using namespace std;

int main(){

  int x,y;

  if (x>0 && x<1000 && x>10){
    x+=1000;
    // GV++;
    printf("data.setup(\"0\", 0).send();");
    printf("\nx is: %d\n",x);
    if (y>-1 && y<100){
      y++;
      //data.setup(0, 0).send();
      printf("\ny is: %d\n",y);
    }
  }
  return 0;
}
