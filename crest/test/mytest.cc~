#include <crest.h>
#include <stdio.h>

int A[100];
char B[12][97];

int main(void) {
int x;
int y;

if (x>0 && x<10)
{
 if (y>x){
	printf ("y is greater than x. x: %d, y:%d", x, y);
 }else{
	printf ("x is greater than or equal to y. x: %d, y:%d", x, y);
 }
}

}
